# CAM-WORKBENCH-INSPECTOR-SELECTION Plan

1. Add generic target resolution and overlay behavior in the package.
2. Add a minimal selected-target sidebar.
3. Mark the first semantic host targets in `apps/web`.
4. Add `Open in Workbench` bridge behavior through the integration layer.
5. Verify build safety and local dev activation behavior.
