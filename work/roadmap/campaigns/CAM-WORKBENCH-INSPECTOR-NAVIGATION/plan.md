# CAM-WORKBENCH-INSPECTOR-NAVIGATION Plan

## Steps

1. Normalize semantic targets into a generic graph inside the package.
2. Replace the flat target browser with a recursive hierarchy tree.
3. Keep expand/collapse independent from selected target.
4. Make search reveal matching targets together with ancestor context.
5. Verify build and browser behavior on live targets plus non-mounted semantic nodes.
