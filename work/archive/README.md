# Archive

Completed campaigns live here.

- `index/` contains short summaries in batches of 10 campaigns.
- `campaigns/` contains per-campaign closeout.

Policy:
- A completed campaign must have `closeout.md` with a single task list.
