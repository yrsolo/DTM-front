# @dtm/workbench-source-analysis

Node-side source analysis for Workbench Authoring.

This package owns AST parsing, module traversal, placement normalization, canonical source identities, and canonical `SourceGraph` construction.
