# @dtm/workbench-contracts

Shared Workbench Authoring contracts.

This package is intentionally runtime-agnostic. It contains only ids, graph models, authoring contracts, and related shared types used by browser/runtime and node-side packages.
