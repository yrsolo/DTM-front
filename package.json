{
  "name": "dtm-front",
  "private": true,
  "scripts": {
    "validate:schema": "node scripts/validate_schema.mjs",
    "check:mojibake": "node scripts/check_mojibake.mjs"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
