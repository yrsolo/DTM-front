# Troubleshooting

??????????:
- ??????? ??????? ???????? runtime, deploy ? data-loading.

Source of truth:
- `apps/web/src/config/publicConfig.ts`
- `apps/web/src/config/runtimeContour.ts`
- `apps/web/src/data/useSnapshot.ts`
- `apps/web/src/data/runtimeDefaults.ts`

## ???????: ?????? ????? ??? ?????? timeline

?????????:
1. ??????????? ?? `config/public.yaml` ??? `config/public.yam` ???????????? ???????? app base.
2. ???? ?? ???????? snapshot request ? Network.
3. ?? ??? ?? snapshot cache ? `dtm.snapshot.v1`.
4. ?? ??????? ?? display limit ?? ???? ??? ????? ????.

## ???????: ?????? ?? ???????? ?? production API

?????????:
1. ????? runtime config ?????? ?? ????????.
2. ??? ?? backend endpoint ? ???????? ?? ????????? route.
3. ?? ??????? ?? test contour ?????? production.
4. ????????? ?? `api_frontend_path` ? ???????? backend path.

## ???????: runtime config ?? ???????????

`publicConfig.ts` ???? ?????? ? ????? ???????:
1. `config/public.yaml`
2. `config/public.yam`
3. ?????????? fallback

???? ?? ???? ??????? ?? ????????:
- ????????? ?????????? ???????? `config/`;
- ????????? MIME ? ?????????? SPA fallback ?? `config/*`.

## ???????: ???? ? `/test/` ???????????, ?? ?? ????????? assets ??? config

?????????:
1. Test frontend ????????????? ??????????? ? ????????? bucket `dtm-front-test`.
2. Gateway ?? ?????????? `/test/{path}` ? prod bucket.
3. `config/public.yaml` ? `config/public.yam` ???? ? ????? test bucket.
4. ? ???? ?? ???????? ?????????? ????? ???? `/config/...`, `/data/...`, `/favicon.ico`, `/DTM_lo.mp4` ???, ??? ?????? ???? ????????????? public asset paths.

## ???????: ????????? frontend ????????? ?? ????

????????? ?????????:
- localhost ????????? `test` contour ??? auth/api;
- ????????? SPA base path ???????? `/`;
- login URL ?????? ????? ? `https://dtm.solofarm.ru/test/ops/auth/login?...`.

?????????:
1. `runtimeContour.ts` ??????? localhost test-????????.
2. `getFrontendBasePath()` ?? localhost ?????????? `/`, ? ?? `/test/`.
3. `getAuthBasePath()` ? `getApiProxyBasePath()` ?? localhost ???? `/test/ops/auth` ? `/test/ops/api`.

## ???????: OAuth callback ?? ?????????

Canonical callback URLs:
- prod: `https://dtm.solofarm.ru/ops/auth/callback`
- test: `https://dtm.solofarm.ru/test/ops/auth/callback`

?????????:
1. ??? test contour ???????????? `YANDEX_CLIENT_ID_TEST` ? `YANDEX_CLIENT_SECRET_TEST`.
2. ??? prod contour ???????????? `YANDEX_CLIENT_ID_PROD` ? `YANDEX_CLIENT_SECRET_PROD`.
3. ? ?????????? Yandex OAuth app ??????????????? ??? ?? callback URL, ??????? ??????? ???????? auth function.

## ???????: ???????? ? ??????????

???? ? UI ??? ? markdown ????? ??????? ???? `?...`, ?????????:
- ???????? ????????? ?????;
- ?? ??? ?? ???? ???????? ? ANSI/Windows-1251;
- ?? ????????? ?? ???? shell-???????? ??? ?????? UTF-8.

???????? ????????:
- `npm run check:mojibake`
